# Tessera's virtual camera.
#
# exclusive_caps=1 is what makes Firefox, Chrome and Zoom accept the device as
# a real camera; without it they list it but show no picture.
options v4l2loopback devices=1 exclusive_caps=1 card_label="Tessera Camera"
